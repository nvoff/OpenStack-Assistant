# OpenStack Bot 🤖

Telegram-бот для управления OpenStack облачной инфраструктурой. Позволяет удобно управлять виртуальными машинами, сетями, дисками, маршрутизаторами и другими ресурсами OpenStack через интерфейс Telegram.

## 📋 Описание

**OpenStack Bot** - это полнофункциональный Telegram-бот, который предоставляет интуитивный интерфейс для управления облачной инфраструктурой. Бот поддерживает русский язык и содержит всю необходимую функциональность для администрирования OpenStack проекта.

## ✨ Основные возможности

### 🖥️ Управление инстансами (виртуальными машинами)
- **Просмотр** списка всех инстансов с их статусами
- **Создание** новых инстансов с выбором:
  - Имени и количества
  - Типа инстанса (flavor)
  - Образа (image)
  - Сети и групп безопасности
  - SSH-ключей
- **Управление состоянием**: запуск, остановка, перезагрузка
- **Удаление** инстансов (с подтверждением)
- **Просмотр деталей**: IP-адреса, размер, образ, группы безопасности
- **Поиск** инстансов по имени, IP, ID или статусу

### 🏷️ Управление тегами
- **Создание и назначение** тегов серверам
- **Удаление** тегов
- **Поиск** инстансов по тегам
- Просмотр всех тегов проекта

### 💾 Управление дисками и снимками
- **Просмотр** списка дисков с размерами и статусами
- **Создание** новых дисков
- **Изменение размера** дисков (расширение)
- **Удаление** дисков
- **Подключение/отключение** дисков к/от инстансов
- **Создание снимков** (snapshots) дисков
- **Управление снимками**: просмотр, редактирование, удаление
- **Создание дисков** из снимков

### 🔐 Управление SSH-ключами
- **Импорт** SSH-ключей в OpenStack
- **Просмотр** списка ключей
- **Удаление** ключей (одного или нескольких)
- Поддержка ключей: RSA, Ed25519, DSS, ECDSA

### 🌐 Управление сетями и маршрутизацией
- **Создание и удаление** сетей
- **Управление подсетями**
- **Работа с маршрутизаторами**:
  - Создание и удаление маршрутизаторов
  - Управление шлюзами (gateway)
  - Добавление и удаление интерфейсов
  - Добавление статических маршрутов
  - Управление "Allowed Address Pairs"
- **Управление Floating IP**:
  - Выделение новых Floating IP
  - Привязка к инстансам
  - Отвязка от инстансов
  - Удаление Floating IP
- **Управление Fixed IP**: добавление и удаление

### 🔍 Поиск и фильтрация
- Поиск по имени, IP, ID инстансов
- Фильтрация по статусу
- Фильтрация по тегам

### 📊 Информация о проекте
- **Квоты проекта**: текущие лимиты на ресурсы
- **Использование ресурсов**: статистика по CPU, памяти, дискам, сетям
- **Информация о проекте**: название, ID, домен, описание
- **Участники проекта** и их роли (при наличии прав)

## 🛠️ Технологический стек

- **Python 3.12+**
- **python-telegram-bot** - для интеграции с Telegram
- **requests** - для HTTP-запросов к OpenStack API
- **python-dotenv** - для работы с переменными окружения

## 📦 Требования

- Python 3.12 или выше
- Доступ к OpenStack API (Keystone, Nova, Cinder, Neutron)
- Telegram Bot Token
- OpenStack учетные данные (username, password, project name, auth URL)

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/nvoff/OpenStack-Assistant.git
cd OpenStack-Assistant
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Конфигурация

Создайте файл `.env` в корне проекта:

```env
# Telegram Bot Token
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# OpenStack Credentials
OPENSTACK_USERNAME=your_username
OPENSTACK_PASSWORD=your_password
OPENSTACK_PROJECT_NAME=your_project_name
OPENSTACK_AUTH_URL=https://your-openstack-api.example.com:5000/v3
OPENSTACK_REGION=RegionOne

# Authorized Users (Telegram User IDs, через запятую)
AUTHORIZED_USERS=123456789,987654321
```

### 5. Запуск бота

```bash
python main_ru.py
```

Бот будет запущен и готов к использованию. Напишите `/start` боту в Telegram.

## 📝 Файлы проекта

- **`main_ru.py`** - главный файл приложения, содержит основную логику и маршрутизацию
- **`instance_menu.py`** - управление инстансами (создание, удаление, управление)
- **`disk_menu.py`** - управление дисками и снимками
- **`network_menu.py`** - управление сетями, маршрутизаторами и IP-адресами
- **`router_menu.py`** - детальное управление маршрутизаторами
- **`keypair_menu.py`** - управление SSH-ключами
- **`tags_manager.py`** - управление тегами инстансов
- **`search_menu.py`** - функциональность поиска
- **`project_menu.py`** - информация о проекте, квоты и использование ресурсов
- **`requirements.txt`** - список зависимостей Python

## 🔐 Безопасность

- Бот проверяет авторизацию пользователя перед каждой операцией
- Список авторизованных пользователей хранится в переменной `AUTHORIZED_USERS`
- Все операции логируются в файл `back/openstack_bot.log`
- Токены и учетные данные хранятся в переменных окружения (не в коде)

## 🗂️ Структура проекта

```
OpenStack-Assistant/
├── main_ru.py                    # Основной файл приложения
├── instance_menu.py              # Управление инстансами
├── disk_menu.py                  # Управление дисками
├── network_menu.py               # Управление сетями
├── router_menu.py                # Управление маршрутизаторами
├── keypair_menu.py               # Управление SSH-ключами
├── tags_manager.py               # Управление тегами
├── search_menu.py                # Поиск и фильтрация
├── project_menu.py               # Информация о проекте
└── .env                          # Конфигурация (не коммитится)

## 📖 Использование

### Основная навигация

1. Напишите боту `/start` для запуска
2. Выберите нужный раздел из главного меню
3. Используйте inline-кнопки для навигации
4. Отправляйте текстовые команды когда это требуется

### Примеры операций

**Создание инстанса:**
1. Главное меню → Управление инстансами → Создать инстанс
2. Введите имя, количество, описание
3. Выберите тип, образ, сеть и другие параметры
4. Подтвердите создание

**Поиск инстанса:**
1. Список инстансов → 🔎 Поиск
2. Выберите критерий (имя, тег, статус)
3. Введите значение для поиска
4. Просмотрите результаты


## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Смотрите файл `LICENSE` для подробностей.
